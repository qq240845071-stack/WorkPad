# 每日收尾摘要

## 2026-05-23

### 本次改动概述

- 增加状态版本号和写入冲突保护：前端保存数据时会带上当前 `stateRevision`，后端发现旧页面覆盖新数据时返回 409，并把最新数据同步回前端。
- 增强持久化合并逻辑：保存项目、公共提醒和推送记录时，保留提醒发送状态、确认状态、回执状态和推送历史，降低旧页面把线上提醒状态覆盖掉的风险。
- API 响应增加 `no-store` 缓存控制，避免登录、状态接口被浏览器或中间层缓存。
- 前端在页面重新获得焦点、从后台切回或浏览器恢复页面时自动刷新远端状态，并在部署版本变化时自动刷新页面。
- 产品工艺卡的复选框字段支持多个可选项，保存时会校验必填项并记录多选结果。
- 新增共享用户名规则模块，统一姓名转拼音账号、默认初始密码和用户名预览逻辑。
- 界面版本号更新为 `WorkPad v2026-05-23`，`package.json` 和 `package-lock.json` 版本同步为 `2026.5.23`。

### 提交信息

- `chore: daily wrapup 2026-05-23`

### 验证

- 已通过：`npm run check`
- 已通过：`node --check shared/username-rules.js`
- 已通过：状态合并脚本验证，旧页面保存不会覆盖已发送的项目提醒、公共提醒和推送记录。
- 已完成：Vercel 体验环境部署，预览地址 `https://workpad-67t5qy1z8-qq240845071-3969s-projects.vercel.app`

### 风险或待办

- 当前仓库没有 `npm run release:version` 脚本，本次按收尾规则手动同步界面版本号和包版本号。
- 本次会部署 Vercel 预览环境，不执行生产环境发布。
